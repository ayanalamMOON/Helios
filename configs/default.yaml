atlas:
  data_dir: "/var/lib/helios"
  aof_fsync: "every"     # options: "every", "interval", "none"
  snapshot_interval_cmds: 10000
  snapshot_interval_time: "5m"

  # Raft consensus configuration (for multi-node clusters)
  raft:
    enabled: false                    # Enable Raft replication
    node_id: "node-1"                # Unique node identifier
    bind_addr: "127.0.0.1:7000"      # Raft RPC listen address
    data_dir: "/var/lib/helios/raft" # Raft data directory (logs, snapshots)
    heartbeat_timeout: "100ms"       # Leader heartbeat interval
    election_timeout: "500ms"        # Follower election timeout base
    snapshot_threshold: 10000        # Commands before triggering snapshot

    # Peer nodes in the cluster (excluding this node)
    peers:
      # - id: "node-2"
      #   address: "127.0.0.1:7001"
      # - id: "node-3"
      #   address: "127.0.0.1:7002"

gateway:
  listen: ":8443"
  tls_enabled: false
  tls_cert: "/etc/helios/cert.pem"
  tls_key: "/etc/helios/key.pem"
  jwt_secret: "change-this-in-production"
  jwt_expiry: "15m"
  rate_limit:
    default_capacity: 100
    default_rate_num: 1
    default_rate_den: 1

proxy:
  listen: ":8080"
  health_check_interval: "5s"
  circuit_breaker:
    failure_threshold: 5
    cooldown_sec: 60

worker:
  poll_interval: "5s"
  max_attempts: 3
  visibility_timeout: 300  # seconds

observability:
  log_level: "info"  # debug, info, warn, error, fatal
  metrics_enabled: true
  metrics_port: ":9090"
  tracing_enabled: false
  tracing_endpoint: "http://localhost:14268/api/traces"

# Horizontal sharding configuration (for large-scale deployments)
sharding:
  enabled: false                    # Enable horizontal sharding
  virtual_nodes: 150                # Virtual nodes per physical node (higher = better distribution)
  replication_factor: 1             # Number of replicas per key (1 = no replication)
  migration_rate: 1000              # Keys per second during migration
  auto_rebalance: false             # Automatically rebalance when nodes added/removed
  rebalance_interval: "1h"          # How often to check for rebalancing needs

# GraphQL API configuration
graphql:
  enabled: true                     # Enable GraphQL API endpoint
  endpoint: "/graphql"              # GraphQL API endpoint path
  playground_enabled: true          # Enable GraphQL Playground UI
  playground_endpoint: "/graphql/playground"  # Playground UI endpoint path
  introspection_enabled: true       # Enable schema introspection (disable in production)
  max_query_depth: 10               # Maximum query nesting depth
  max_complexity: 1000              # Maximum query complexity score
  allowed_origins:                  # CORS allowed origins
    - "*"                           # Allow all origins (restrict in production)
  schema_docs:
    enabled: true                   # Enable automatic schema documentation generation
    auto_generate: true             # Generate schema docs at startup/first use
    default_format: "markdown"      # markdown or json
    include_schema_sdl: false       # Include raw SDL in generated docs
    enable_http_handler: true       # Expose /graphql/docs endpoint
    auto_export: false              # Auto-write docs to file after generation
    export_path: ""                 # Optional output file path for schema docs
  federation:
    enabled: true                   # Enable GraphQL federation support (_service/_entities)
    service_name: "helios"         # Service name exposed to supergraph composition
    service_version: "1.0.0"       # Service version metadata
    include_service_sdl: true       # Expose schema SDL via _service.sdl
    strict_entities: false          # If true, unknown/invalid entities fail the entire request
    entity_types:                   # Allowed federated entity types
      - "User"
      - "KVPair"
      - "Job"
      - "ShardNode"
      - "RaftPeer"

# Plugin framework configuration
plugins:
  enabled: false                   # Enable plugin manager globally
  default_timeout: "150ms"        # Per-plugin hook timeout if not overridden
  default_fail_open: true          # If true, plugin failures do not block core operations
  event_buffer: 1024               # Event queue capacity for async event subscribers
  plugins:
    # - name: "audit"
    #   enabled: true
    #   priority: 100
    #   fail_open: true
    #   max_failures: 5
    #   cooldown: "30s"
    #   settings:
    #     max_entries: 1000
    #     include_values: false
    #     max_value_preview_bytes: 96
    #     command_types: ["SET", "DEL", "EXPIRE"]
    #
    # - name: "keyguard"
    #   enabled: true
    #   priority: 200
    #   fail_open: false
    #   settings:
    #     max_key_length: 256
    #     max_value_bytes: 1048576
    #     denied_prefixes: ["internal:", "system:"]
    #     denied_suffixes: [":tmp"]
    #     allowed_commands: ["SET", "GET", "DEL", "TTL", "EXPIRE"]
    #     max_ttl: 86400
    #
    # - name: "traffic_guard"
    #   enabled: true
    #   priority: 150
    #   fail_open: false
    #   settings:
    #     window: "1s"
    #     max_commands: 500
    #     max_buckets: 20000
    #     scope: "session"          # session | key | session_key | global | command
    #     applies_to: ["SET", "DEL", "EXPIRE"]
    #
    # - name: "http_access"
    #   enabled: true
    #   priority: 50
    #   settings:
    #     skip_paths: ["/health", "/metrics"]
    #     include_user_agent: true
    #     include_remote_addr: true
    #     emit_request_id: true
    #     request_id_header: "X-Request-ID"
    #     event_name: "http.request.completed"

